Qno.242  Valid Anagram (easy)

cat same as act

solution: 
  sort the string and compare 

python code:
class Solution(object):
    def isAnagram(self, s, t):
        """
        :type s: str
        :type t: str
        :rtype: bool
        """
        if len(s) != len(t):
            return False
        
        p = sorted(s)
        q = sorted(t)

        if p == q:
            return True
        else:
            return False
  
